{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch {{ .AppName }}",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}"
        }
    ]
}

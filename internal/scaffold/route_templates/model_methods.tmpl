{{ range .Methods }}
// {{ .ReqTypeName }} is the {{ .BodyDescription }} for {{ .Verb }} {{ $.Route }}.
{{- if $.PathParams }}
// Path parameter(s) {{ $.PathParamsCSV }} are populated by request.DecodePath —
// add a matching json-tagged field per name to receive them.
{{- end }}
type {{ .ReqTypeName }} struct {
	// TODO: add fields.
}

// {{ .RespTypeName }} is the response payload for {{ .Verb }} {{ $.Route }}.
type {{ .RespTypeName }} struct {
	// TODO: add fields.
}
{{ end }}